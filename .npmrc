# Suprimir aviso de audit en CI — la vulnerabilidad de esbuild afecta solo
# al servidor de desarrollo local, no a builds de producción.
# No hacer npm audit fix --force: instalaría Vite 8 (breaking change).
audit=false
